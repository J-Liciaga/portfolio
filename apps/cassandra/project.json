{
  "name": "cassandra",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/cassandra/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/cassandra",
        "format": ["cjs"],
        "bundle": false,
        "main": "apps/cassandra/src/main.ts",
        "tsConfig": "apps/cassandra/tsconfig.app.json",
        "assets": ["apps/cassandra/src/assets"],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          }
        }
      },
      "configurations": {
        "development": {},
        "production": {
          "generateLockfile": true,
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            }
          }
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cassandra:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cassandra:build:development"
        },
        "production": {
          "buildTarget": "cassandra:build:production"
        }
      }
    },
    "docker-build": {
      "dependsOn": ["build"],
      "command": "docker build -f apps/cassandra/Dockerfile . -t cassandra"
    }
  }
}
